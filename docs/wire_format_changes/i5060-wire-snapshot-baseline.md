# Wire format snapshot baseline

Adds the wire format changes detection: a registry of node-to-node types and a
golden snapshot of their shape. This records the current wire types as the
baseline. No wire format changed, so old and new nodes remain compatible.
